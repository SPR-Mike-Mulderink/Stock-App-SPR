import { STOCK_DATA } from './types';
import { useFetch } from '../hooks'

export const stockData = () => {
  const data = null;
  return {
    stocks: data,
    user_name: 'Mike',
    type: null
  }
}